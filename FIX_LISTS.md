[x] เติมหัวข้อ ตั้งค่าทีม อยู่ข้างบนตัวกรอกจำนวนทีม
[x] เปลี่ยนชื่อจากจำนวนช่องทั้งหมด เป็นตั้งค่าเกม
[x] เปลี่ยนชื่อปุ่มเป็น ขั้นต่ำ แนะนำ เอาปุ่ม Auto ออก (ปุ่มไหนถ้ากดไม่ได้ให้เป็น disable แทนการหายไป)
[x] ช่องขั้นต่ำต้องนับรวมระเบิด glitch ด้วยนะ เช่น ระเบิด 5 glitch 1 การ์ด 7 ขั้นต่ำต้อง 13 ตอนนี้ปรับไป 12 ได้ซึ่งไม่ถูก
[x] Glitch bomb สามารถตั้งค่าได้ว่าไม่ให้ใช้ item กี่ turn
[x] เวลาต่อรอบให้ช่องกรอกอยู่ต่อจาก text ในบรรทัดเดียวกัน เช่น "เวลาต่อรอบ ช่องกรอกตัวเลข วินาที" เวลาตัดสายระเบิดก็ทำเหมือนกัน
[x] เปอร์เซ็นต์เสียงซ้ำซ่้อนในตั้งค่าเพิ่มเติม ทำให้อยู่ยรรทัดเดียวกัน "ระดับเสียง Effect bar ช่องกรอกเลข %"
[x] ปุ่มเปลี่ยนธีมบั๊ก เหมือนมีปุ่มซ้อน ในหน้าเล่นเกม
[x] หน้าต่างตอนเล่นเกม ทำไมไม่อยู่กึ่งกลางจอ align vertical center
[x] ตอนกดจบเกมทันที จะไม่แสดงคำว่าเสมอกัน แต่จะเป็น เกมถูกยุติโดนผู้ใช้ และไม่แสดงกราฟ แสดงแต่ score
[x] ตั้งค่าช่องตรงโอกาสโดนระเบิด บาร์จะเปลี่ยนสีตามโอกาสที่จะโดน
[x] เอาบรรทัดที่เขียนว่าสมดุลออก แล้วย้าย text ที่จะบอกความยากง่าย ไปไว้ตรง % จะได้แบบนี้ (ง่ายเกินไป 6%)

---

## บันทึกการแก้ (ครบ 12/12)

| # | แก้ที่ไหน | หมายเหตุ |
|---|---|---|
| 1 | `SetupScreen.tsx` | เพิ่ม `<h2>ตั้งค่าทีม</h2>` เหนือแถวกรอกจำนวนทีม |
| 2 | `SetupScreen.tsx` | หัวข้อ "จำนวนช่องทั้งหมด" → "ตั้งค่าเกม" |
| 3 | `SetupScreen.tsx` | เหลือ 2 ปุ่ม `ขั้นต่ำ N` / `แนะนำ N`, ตัดปุ่ม auto ทิ้ง, ปุ่มที่กดแล้วไม่เปลี่ยนค่า → `disabled` |
| 4 | `SetupScreen.tsx`, `config.ts` | `minOpts.glitchCount` ใช้ `autoGlitch` (นับทั้งโหมด auto และ manual) เดิมนับเฉพาะ manual |
| 5 | `types.ts`, `config.ts`, `engine.ts`, `SetupScreen.tsx`, `RulesContent.tsx` | เพิ่ม setting `glitchLockTurns` (0–10, default 2) แทนเลข 2 ที่เคย hardcode ในเอนจิน |
| 6 | `SetupScreen.tsx` | `NumberField` รับ prop `unit` → เรียงเป็นบรรทัดเดียว "เวลาต่อรอบ [60] วินาที" |
| 7 | `SetupScreen.tsx` | `VolumeField` เหลือเปอร์เซ็นต์ชุดเดียว เรียง label + bar + ช่องกรอก + % บรรทัดเดียว |
| 8 | `GameScreen.tsx` | ปุ่มปิดเสียงย้ายจาก `top-4 right-4` ไป `top-16 right-3` — เดิมทับปุ่มธีมของ `App` |
| 9 | `GameOverScreen.tsx`, `BlockPrompt.tsx`, `globals.css` | เปลี่ยนเป็น `flex` + `m-auto` / `place-items: safe center` — จัดกลางแนวตั้งโดยไม่ตัดหัวเมื่อเนื้อหาสูงเกินจอ |
| 10 | `GameOverScreen.tsx` | เช็ค `USER_ENDED_LOG` → ขึ้น "เกมถูกยุติโดยผู้ใช้" และซ่อนโพเดียม เหลือแต่ตารางคะแนน |
| 11 | `globals.css` | `.range-bar div` เดิมตั้ง `background: var(--primary)` ตายตัวและอยู่นอก `@layer` จึงชนะ utility ของ Tailwind ตลอด → bar ไม่เคยเปลี่ยนสี เอา background ออก |
| 12 | `SetupScreen.tsx` | ตัดบรรทัด "สมดุล" ทิ้ง ย้ายคำบอกความยากไปติดกับ % เป็น `(ง่ายเกินไป 6%)` |

เทส: 227 ผ่าน / 0 ตก (เพิ่มใหม่ 14 ตัว) — `bunx tsc --noEmit` และ `bun run build` ผ่าน
