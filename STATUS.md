recheck หน่อยว่าตอนนี้เป็นยังไง ตอนนี้เหมือนจะไม่มีหน้า index เหมือน repo thai-wordle เลย อยากให้ตั้งค่าก่อนกดเริ่มเกมประมาณนั้น มี menu ให้
  เริ่มเกม, ตั้งค่า, กฎกติกา, clear storage อะไรประมาณนั้น จัดการสร้าง task ให้หน่อย และสร้าง test ด้วยว่าเล่นได้จริง และสามารถ custom
  จำนวนระเบิด glitch ได้ ส่วน alert ให้ใช้ sweet alert และ กลับไปใช้ html ได้ไหม หรือ bun elysia หรือ bun + vite ก็ดีนะ ไม่อยากใช้
  pnpm ซักเท่าไหร่ ตรงสร้างจำนวนทีมให้ default name เป็น ทีม 1, ทีม 2,... และมีช่องให้คีย์ตัวเลขว่าจะปรับให้เหลือกี่ทีม และกดยืนยัน
  เพิ่มคำอธิบายตอนเปิดตั้งค่า shrinking, glitch bomb, card item หน่อย คำว่า attack บั๊ก และแต่ละกลุ่มเปิดเกมไม่ได้ และอยากให้ random
  ลำดับรายชื่อของทีมได้ เพื่อที่จะได้ shuffle ก่อนเริ่มเกมว่าทีมไหนจะได้เริ่มก่อนและตามลำดับ ตอนนี้คลิ๊กเปิดช่องไม่ได้ และเช็ค wording เหมือนคำว่า
  Attack ตัว A จะหายไป และหลักการได้ Card item คืออะไร ได้จากการเปิดช่องใช่ไหม และสามารถตั้งค่าจำนวนการ์ดได้ไหม ปรับ balance ให้หน่อย
  ให้สัมพันธ์กับจำนวน range ด้วยนะ อยากให้จบเกมมี leaderboard ด้วย และเวลานับถอยหลังต้องแสดงด้วย คือแต่ละทีมจะมี action ให้เลือกระหว่างใช้
  item หรือเปิดป้าย ซึ่งบาง item จะ skip turn ไปเลย หรือบางไอเท็มกดใช้แล้วค่อยเปิดช่องต่อก็มี จะใช้กี่ item ก็ได้ แล้วแต่เลยใน turn ตัวเอง
  และสามารถ block ทีมอื่นได้เมื่อทีมนั้นใช้ item ในรอบนั้น